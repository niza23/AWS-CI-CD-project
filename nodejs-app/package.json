{
  "name": "nodejs-cicd-app",
  "version": "1.0.0",
  "description": "Node.js app demonstrating Jenkins CI/CD + AWS EKS + ArgoCD GitOps",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "jest --testPathPattern=src --forceExit --coverage",
    "lint": "eslint src/**/*.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "supertest": "^6.3.3",
    "nodemon": "^3.0.1"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
